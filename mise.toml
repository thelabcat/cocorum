[tools]
python = "3.14"

[env]
_.python.venv = { path = ".venv", create = true }

[tasks.install]
description = "Install dependencies"
alias = "get_deps"
run = """
pip install -U -r requirements.txt
pip install -U build
pip install -U mkdocs "mkdocstrings[python]" mkdocs-material
"""
